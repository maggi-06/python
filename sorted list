#program to sort the elements in the list
ap=[]
n=int(input("enter number of districts:"))
for i in range(n):
    x=input()
    ap.append(x)
ap.sort(reverse=False)
            
print(ap)
OUTPUT:
enter number of districts:4
kakinada
vskp
srikakulam
amalapuram
['amalapuram', 'kakinada', 'srikakulam', 'vskp']
